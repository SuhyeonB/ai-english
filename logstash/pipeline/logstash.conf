input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  mutate {
    add_field => { "app" => "ai-english" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "ai-english-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}